eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
  server:
    enable-self-preservation: false # 用于开发环境，正式环境请注释
spring:
  application:
    name: eureka
server:
  port: 8761